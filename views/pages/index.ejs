<% include ../partials/header %>


  <div class="jumbotron">
    <h2>Create your game</h2>
    <button onclick="startGame(8)"> Start 8x8</button>
    <button onclick="startGame(10)"> Start 10x10</button>
    <button onclick="startGame(16)"> Start 16x16</button>
  </div>

  <div class="row justify-content-md-center">

    <div class="col-sm-4">
      <h3>Open games:</h3>
      <ul>
        <% if (games.size) {
            games.forEach( (game, key) => { %>
          <li>
            <%= key %> (<%= game.size %>x<%= game.size %>) <a href="/<%= key %>/join"> Join</a>
          </li>
          <% }); 
          } else { %>
            <p>No games</p>
          <% } %>
      </ul>

    </div>
    <div class="col-sm-4">
      <h3>Playing right now</h3>

    </div>
  </div>

  <script type="text/javascript">
    function startGame(size) {
      $.get('/start/' + size).done((responce) => {
        location.href = '/' + responce
      })
    };
  </script>

  <% include ../partials/footer %>